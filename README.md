# Digital-Lock-System
digital lock system offers an electronic solution for access control, replacing traditional mechanical locks. This design is implemented using a finite state machine (FSM) model that processes a predefined key sequence serially. Only the correct sequence triggers the 'unlock' signal.
